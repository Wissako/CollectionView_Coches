<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CollectionViewEjemplo.Pages.DetalleCochePage"
             Title="{Binding nombreCoche}"
             BackgroundColor="#F5F5F5">

    <Grid RowDefinitions="*, Auto">

        <ScrollView Grid.Row="0">
            <VerticalStackLayout Spacing="20" Padding="0,0,0,20">

                <Image Source="{Binding cocheUrl}"
                       Aspect="AspectFit"
                       HeightRequest="300"
                       HorizontalOptions="Fill"/>

                <VerticalStackLayout Padding="20" Spacing="10">

                    <Label Text="{Binding nombreCoche}" 
                           FontSize="32"
                           FontAttributes="Bold"
                           TextColor="#333"/>

                    <Label Text="{Binding marcaCoche}" 
                           FontSize="20"
                           TextColor="#666"
                           TextTransform="Uppercase"
                           FontAttributes="Bold"/>

                    <Border BackgroundColor="#E0F7FA" 
                            StrokeShape="RoundRectangle 8" 
                            HorizontalOptions="Start"
                            Padding="12,6"
                            StrokeThickness="0">
                        <Label Text="{Binding Origen}" 
                               TextColor="#006064"
                               FontSize="14"
                               FontAttributes="Bold"/>
                    </Border>

                    <Label Text="Detalles del vehículo..." 
                           TextColor="Gray" 
                           Margin="0,20,0,0"/>
                </VerticalStackLayout>
            </VerticalStackLayout>
        </ScrollView>

        <Button Grid.Row="1"
                Text="Volver al Menú"
                Clicked="OnVolverClicked"
                Margin="20"
                BackgroundColor="#333"
                TextColor="White"
                HeightRequest="50"
                CornerRadius="25"
                FontAttributes="Bold"/>
    </Grid>
</ContentPage>