<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CollectionViewEjemplo.Pages.CollectionViewDemo"
             Title="CollectionViewDemo">
    <CollectionView x:Name="collectionView" ItemsLayout="VerticalGrid" SelectionMode="Single">
        <!-- <CollectionView.ItemsSource>
            <x:Array Type="{x:Type x:String}">
                <x:String>Item 1</x:String>
                <x:String>Item 2</x:String>
                <x:String>Item 3</x:String>
                <x:String>Item 4</x:String>
                <x:String>Item 5</x:String>
                <x:String>Item 6</x:String>
                <x:String>Item 7</x:String>
            </x:Array>
        </CollectionView.ItemsSource>
            -->
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Margin="10" Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Image
                    Source="{Binding cocheUrl}"
                    WidthRequest="60"
                    HeightRequest="50"
                    Aspect="AspectFit"
                    Grid.Column="0" />

                        <VerticalStackLayout Grid.Column="1" Padding="10,0" VerticalOptions="Center">
                            <Label Text="{Binding nombreCoche}"
                           FontSize="Medium"
                           FontAttributes="Bold" />
                            <Label Text="{Binding marcaCoche}"
                           FontAttributes="Italic" />
                        </VerticalStackLayout>
                    </Grid>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>
    

</ContentPage>